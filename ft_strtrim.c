#include "libft.h"
#include <stdlib.h>

size_t		debut(char const *s1, char const *set)
{
	size_t		n;
	int			i;
	int			j;

	n = 0;
	i = 0;
	j = 0;
	while (i == 0)
	{
		if ()
	}
}

char		*ft_strtrim(char const *s1, char const *set)
{
	char	*dest;

	if ((dest = (malloc(sizeof(char) * (debut(s1, set) + fin(s1, set)))))
	{
	}
}
